import os
from telegram import Bot
import asyncio

TOKEN = os.getenv("TOKEN")
CHAT_ID = os.getenv("CHAT_ID")

bot = Bot(token=TOKEN)

async def main():
    await asyncio.sleep(10)
    await bot.send_message(chat_id=CHAT_ID, text="🔥 التجربة شغالة")

asyncio.run(main())